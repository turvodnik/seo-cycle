# Пошаговый план улучшений — данные Эмвуди, 2026-07-05

Каждый шаг: **что → команда → ожидаемый эффект (из аналитики) → метрика контроля**.
Цифры — из живого среза 2026-07-04 (500 запросов Я.Вебмастера) и Монте-Карло
прогноза (`seo-cycle forecast`, P10–P90).

## Что говорят данные сегодня

| Факт | Значение | Вывод |
|---|---|---|
| Видимость | 24 в топ-3 · **419 в топ-10** · ср. позиция 7.8 | база сильная, борьба — за топ-3 |
| Трафик | 111 кликов / 4702 показа за 14 дней (CTR 2.4%) | CTR соответствует позиции ~8; рост = позиции, не сниппеты |
| Потенциал (модель) | сейчас ~3.3k кликов/мес (P10–P90 2.5–4.3k) → **топ-10: 41.7k** (31.8–55.7k) | потолок ×12 — есть за что работать |
| Upside по кластерам | **osp +14.4k** · plywood +9.0k · membranes +8.7k · mineral_wool +4.6k · xps +1.0k | порядок публикаций должен идти по этим цифрам |
| ⚠ Несвязка | forecast видит **0 ranked** из 476 ключей ядра | ядро и живые запросы не сматчены — прогноз оторван от факта |
| SERP-валидация | только 7 запросов | intel (merge/split, AEO-фичи) пока слеп |
| Очередь | 6 approved-ключей, 7 брифов, 2 черновика (гейт не пройден чисто) | конвейер загружен, но не доведён |
| Доступы | Telegram ▫ · Директ ▫ · GBP ▫ | алерты и реклама выключены |

## Шаги

**0. Telegram — 5 минут, разблокирует алерты и отчёты** *(человек)*
`seo-cycle auth login telegram --global` (токен у @BotFather, chat id у @userinfobot).
Метрика: `notify.py --test` → «Отправлено».

**1. Сшить ядро с живыми запросами** *(сегодня, машина)*
`seo-cycle run script semantic-core-resync` → цель: forecast показывает ranked > 300 из 476.
Эффект: прогноз и KPI считаются от реальных позиций, а не от UNRANKED=40.
Метрика: строка `Inputs: … ranked` в `seo-cycle forecast`.

**2. Переупорядочить контент-очередь по upside** *(эта неделя)*
Порядок драфтов по данным, а не по алфавиту:
1) дожать **osp** до passed (+14.4k потенциала, драфт уже есть) → 2) **fanera/plywood** (+9.0k) → 3) **пароизоляция/membranes** (+8.7k) → 4) обновить **минвату** (+4.6k) → 5) **xps** (+1.0k) → 6) акустика (+0.6k).
Команда на каждый: `seo-cycle loop draft <draft.md> --outline <outline.json>` до passed (авто-оценка и RAG-индекс — сами).
Метрика: ≥2 passed-драфта в неделю; `seo-cycle score show` без 🔴.

**3. Накормить SERP-валидацию** *(эта неделя, ~30 мин)*
XMLRiver-экспорт топ-10 по ядру → `serp-validation-import --write` → `seo-cycle intel --write`.
Эффект: merge/split-кандидаты кластеров + AEO-приоритеты (какие запросы несут AI Overview/PAA — туда answer-units в первую очередь).
Метрика: intel показывает ≥100 запросов с SERP, список aeo_priority не пуст.

**4. Включить план-факт** *(после второго среза — расписание снимет его завтра в 6:10)*
Заполнить `kpi.goals` в seo-cycle.yaml (например: топ-3 24→60 к октябрю, клики 240→800/мес) → `seo-cycle kpi --write` еженедельно (алерт при off_track придёт сам после шага 0).
Метрика: kpi-report `on_track`; movers-раздел в `seo-cycle progress` без массовых ↓.

**5. Реклама: защитить топ и добрать несезон** *(после токена Директа — человек + машина)*
`seo-cycle auth login yandex-direct` → `ads fetch --live` → `ads analytics`.
Первая же выгода из данных: 24 запроса в топ-3 — кандидаты «не платить за то, что и так наше» (правило 1), конверсионные термы вне ядра — в семантику (правило 2).
Метрика: ads-analytics.md с заполненными правилами; wasted spend → минус-слова.

**6. Техническая гигиена сайта** *(один прогон, потом ежемесячно)*
`seo-cycle crawl --live && seo-cycle structure && seo-cycle links --live`.
Эффект: битые ссылки/дубли title/noindex до того, как их найдёт Яндекс; мёртвые источники в статьях — до потери E-E-A-T.
Метрика: findings critical/error = 0 в `seo/crawl/site-crawl.md`.

**7. GBP + GEO-табло** *(человек 40 мин + машина)*
Подача verification по [gbp-verification-submission.md](gbp-verification-submission.md); до одобрения — Testing-режим. Параллельно старт журнала цитируемости: `seo-cycle geo-log --record …` после каждой AI-проверки бренда.
Метрика: gbp-health `available`; тренд в `seo-cycle geo-log` ≥1 замер/мес на движок.

**8. Портфель: включить остальные проекты** *(по 20 мин на проект)*
gsse.ru — тем же путём, что Эмвуди (webmaster-fetch → snapshot → db); PifagorLab (eu) — через GSC (`GSC_SITE_URL` + service account).
Метрика: `seo-cycle progress --global` — 3/3 проекта со срезами; пересечения запросов = 0 (или осознанны).

**9. Месячный контур целиком**
`seo-cycle run monthly` (4 системы с approvals) + `seo-cycle report --write --pdf --send` клиенту.
Метрика: месячный дашборд без просроченных approvals; отчёт ушёл в Telegram.

**Инженерный трек (фоном):** ~~JS-юниты рендеров~~ — сделано (v1.89); поднять in-process coverage 52%→65% переводом subprocess-тестов ядра на прямые импорты; bump GitHub actions (checkout v5) — снимет Node20-предупреждение.

## Ритм контроля

- **Ежедневно** (автоматом, 6:10): срез позиций → дельты в дашборде.
- **Еженедельно** (пн 6:30 автоматом + 10 мин глазами): портфель, movers, approvals, `score show`.
- **Ежемесячно**: monthly-цикл, crawl, отчёт клиенту, ревизия этого плана по свежему forecast.
